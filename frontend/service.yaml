apiVersion: v1
kind: Service
metadata:
  name: app-frontend
  namespace: default
  labels:
    app: app
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: app
    component: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
