apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
  namespace: default
  labels:
    app: todo
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: todo
    component: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
