apiVersion: v1
kind: Service
metadata:
  name: todo-backend
  namespace: default
  labels:
    app: todo
    component: backend
spec:
  type: ClusterIP
  selector:
    app: todo
    component: backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
